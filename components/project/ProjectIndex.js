import React from 'react';
import Navbar from '../shared/Navbar';

const ProjectIndex = () => {
    return (
        <div className="app">
            <Navbar></Navbar>
        </div>
    );
};

export default ProjectIndex;